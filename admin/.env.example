NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:5000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Sunucu tarafı backend adresi. Check-up ekranları personel oturumunu her istekte
# backend'e (/api/admin/auth/verify) sorarak doğrular. Boşsa NEXT_PUBLIC_BACKEND_URL kullanılır.
BACKEND_URL=http://127.0.0.1:5000

# Check-up veritabanı (kocumnet_checkup). Şemanın ve migration'ların sahibi app/;
# yönetim paneli bu veritabanına yalnızca okur/yazar, şemayı DEĞİŞTİRMEZ.
CHECKUP_DATABASE_URL=postgresql://KULLANICI:PAROLA@localhost:5432/kocumnet_checkup?schema=public
